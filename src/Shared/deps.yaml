deptrac:
  layers:
    - name: Shared
      collectors:
        - type: className
          regex: App\\Shared\\.*

    - name: Vendor
      collectors:
        - type: className
          regex: ^(Symfony|Doctrine|Gesdinet|Ramsey).*

  ruleset:
    Shared:
      - Vendor